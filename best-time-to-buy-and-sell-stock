class Solution:
    def maxProfit(self, prices):
        mn = prices[0]
        ans = 0

        for p in prices:
            if p < mn:
                mn = p
            elif p - mn > ans:
                ans = p - mn

        return ans
